# AI Agent Swarm V2 — Dependências
# Instale com: pip install -r requirements.txt

# Discord
discord.py>=2.3.0

# LLM (chamadas via OpenAI-compatible API — sem pacote g4f necessário)
# As chamadas são feitas diretamente via urllib (built-in)

# Geração de mídia
diffusers>=0.33.0
transformers>=4.52.0
accelerate>=1.6.0
torch>=2.7.0
torchvision
imageio[ffmpeg]
Pillow>=11.0.0
safetensors
sentencepiece
huggingface_hub>=0.31.0

# Utilitários
python-dotenv>=1.0.0
